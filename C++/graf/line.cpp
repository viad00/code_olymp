#include <iostream>
#include <graphics.h>
#include <conio.h>

using namespace std;

int main() {
    initwindow(300,300);
    setcolor(2);
    line(50,50,200,200);
    system ("pause");
    closegraph();
    return 0;
}
